#!/usr/bin/env zsh
set -euo pipefail

if pgrep -x Dia >/dev/null; then
  pkill -TERM -x Dia

  for _ in {1..20}; do
    pgrep -x Dia >/dev/null || break
    sleep 0.5
  done
fi

/Users/eli/Dev/minimal-twitter-fork/scripts/start-dia "$@"
